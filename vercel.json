{
    "buildCommand": "echo 'No build needed for static HTML'",
    "outputDirectory": "frontend/public",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}